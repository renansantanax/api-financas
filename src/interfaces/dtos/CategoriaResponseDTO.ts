/*
    DTO para representar os dados que serão
    retornados na consulta de categorias
*/
export interface CategoriaResponseDTO {
    id: number; //id da categoria
    nome: string; //nome da categoria
}